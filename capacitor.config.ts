import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.yourcompany.newsthumbnailmaker",
  appName: "News Thumbnail Maker",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
